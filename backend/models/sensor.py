class Sensor:
    def __init__(self, sensor_id, robot_id, sensor_name, sensor_type):
        self.sensor_id = sensor_id
        self.robot_id = robot_id
        self.sensor_name = sensor_name
        self.sensor_type = sensor_type
